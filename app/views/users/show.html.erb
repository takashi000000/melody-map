<h1>users#show</h1>
<%= image_tag @user.image_url, size: "250x200" if @user.image? %>
<p>名前 : <%= @user.name %></p>
<p>メールアドレス : <%= @user.email %></p>
<p>プロフィール : <%= @user.profile %></p>

<% if current_user.id == @user.id %>
  <%= link_to "編集する", edit_user_registration_path %>
<% end %>

<h2>ユーザーの投稿一覧</h2>
<% @user.krks.each do |t| %>
  <%= t.user.name %>
  <%= t.name %>
  <%= t.about %>
<% end %>

<!-- 以下はいいね機能を実装している場合-->
<h2>ユーザーがいいねした投稿一覧</h2>
<% @user.liked_krks.each do |t| %>
  <%= t.user.name %>
  <%= t.name %>
  <%= t.about %>
<% end %>
<!-- ここまで-->

<%= link_to "Tweet一覧に戻る", krks_path %>